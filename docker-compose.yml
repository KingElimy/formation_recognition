version: '3.8'

services:
  formation-api:
    build: ..
    ports:
      - "8000:8000"
    environment:
      - LOG_LEVEL=info
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  # 可选：Redis用于缓存
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"